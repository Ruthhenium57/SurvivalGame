// Fill out your copyright notice in the Description page of Project Settings.


#include "SurvivalLocalPlayer.h"

void USurvivalLocalPlayer::PostInitProperties()
{
	Super::PostInitProperties();

	// if (USurvivalSettingsLocal* LocalSettings = GetLocalSettings())
	// {
	// 	LocalSettings->OnAudioOutputDeviceChanged.AddUObject(this, &USurvivalLocalPlayer::OnAudioOutputDeviceChanged);
	// }
}
